# v0.1.0: independent Rust-host baseline

Status: in preparation. The workspace and `VERSION` stamp target v0.1.0;
the latest published release remains v0.1.0-rc.4. Tag the release commit
only after all required gates below are green on that candidate.

> The first independent Rust-host release with a stable host/runtime boundary
> and a native performance-observability workflow.

The RC series established migration, parity, and ownership. v0.1.0 makes the
independent Rust host the product: runtime, scheduling/policy, serving,
KV/state, distributed orchestration, observability, and performance control.
CUDA, MMQ, VMM, and hardware-specific kernels remain native compute.
The boundary follows the existing [architecture](../rust-migration/ARCHITECTURE.md)
and [opaque FFI contract](../rust-migration/FFI_CONTRACT.md).

`ds4-perf` is the first performance subsystem owned by this independent host.
It turns existing benchmark/profiler practice into a reproducible Rust workflow:

```text
ds4-bench -> ds4-perf scout -> nsys + NVTX -> diagnosis
          -> targeted ncu -> code change -> same-workload A/B + correctness proof
```

This is an initial stable baseline, not a claim that engine development is
finished. Stability requires both the host/runtime contract and the production
evidence; adding a profiler alone does not satisfy the release bar.

## Required gates

The ds4-perf results below were obtained on GB10 against the implementation
recorded in `9269100`, before the version-only preparation change. Historical
RC/campaign results are references, not a new release-candidate qualification.

| Gate | Current evidence / remaining work |
|---|---|
| Production `ds4` Rust host | PENDING: native build passed; qualify the production CLI on the release candidate. |
| `ds4-server` | PENDING: native build passed; qualify the documented API/serving lanes on the candidate. |
| `ds4-bench` | Qwen 2K/8-token smoke PASS; broader candidate workloads remain pending. |
| Supported model families' major live gates | PENDING: use the [current family scope](../../README.md#supported-model-families) and exact validated artifacts. |
| CUDA capture/eager correctness | PENDING: run the existing [proof gates](../rust-migration/BASELINE.md) on the candidate. |
| Long context | PENDING: revalidate the claimed family/context shapes; short scout coverage does not close this gate. |
| Memory/residency behavior | PENDING: owner/worker, KV and serving policy qualification. The [Solar 64K host freezes](../solar-open2-optimization-2026-09-07.md#campaign-closure-and-limits) remain unresolved. |
| No material regression against RC.4 | PENDING: same fixture, model, policy, context, output length, cache protocol and fresh-process A/B. |
| `ds4-perf doctor` | PASS: selected benchmark's capability; profiled build READY, ordinary build explicitly DEGRADED. |
| `ds4-perf scout` | PASS: fresh baseline and capture, preserved arguments/environment, clear failure/degradation. |
| Official Rust NVTX | PASS: optional `nvtx` 2.0.0 in `ds4-cli`, only `std`; direct `LocalRange`, unchanged inference ABI. |
| Nsight normalization and phase attribution | PASS: `ds4.prefill` / `ds4.decode`; all 99 phase kernel groups matched an independent export. |
| Conservative diagnosis | PASS: model-free host-idle/fragmentation/dominant/mixed/unknown tests; live host-idle and dominant-kernel attribution. |
| Raw profiler evidence | PASS: `.nsys-rep`, raw reports and normalized CSV retained outside Git. |

The [local scout validation](../prefill-decode-optimization-playbook.md#local-scout-with-ds4-perf)
records byte-identical 248,320-element frontier logits and eight greedy tokens
across six runs. Optional-NVTX versus ordinary builds, both without Nsight,
differed by -0.13% prefill / -0.44% decode in median throughput. This measures
the instrumentation configuration, **not** regression against RC.4.

For each remaining gate, record the candidate SHA/build, fixture and workload,
environment/cache policy, command, result, and retained evidence path. Preserve
the original goldens and distinguish existing C-shared failures from new drift.
The [parity matrix](../rust-migration/PARITY_MATRIX.md) and
[split evidence](../rust-migration/SPLIT_READINESS.md) remain reference protocols.

## Deferred to v0.1.x

`ds4-perf ncu`, `compare`, `regression`, and `tune`; a direct CUPTI collector;
automatic kernel tuning. These are follow-up candidates, not v0.1.0 blockers.
The first release only suggests bounded NCU commands and requires every code
change to return to the existing benchmark and correctness gates.
